## GNN训练理论
可以。这里要先区分两层含义：

> 论文 3.2.1 里讲的 **GNN 前向传播 / 反向传播** 是通用图神经网络的理论过程；而论文 3.3.1 真正用于 AUV 定位的是 **GCN + BN + Dropout + FC** 组成的具体网络。论文图 3-4 显示，该 GNN 协同定位网络由三组“GCN + BN + Dropout”的图卷积层，再接全连接层输出二维坐标。

下面我按这个实际网络来解释。

---

# 1. 整体流程先看一遍

论文中的 GNN 定位网络可以理解为：

```text
原始数据：
x, y, v, a, θ, delay
    ↓
输入标准化
    ↓
构建图 G = {X, A}
    ↓
第 1 层：GCN → BN → ReLU → Dropout
    ↓
第 2 层：GCN → BN → ReLU → Dropout
    ↓
第 3 层：GCN → BN → Dropout
    ↓
FC 全连接层
    ↓
输出预测坐标：
x_{t+1}, y_{t+1}
    ↓
MSE 损失
    ↓
反向传播更新参数
```

论文之所以用这个结构，是因为它希望 GNN 学习不同传感器变量之间的关系，从而融合异源异构信息；同时考虑到 AUV 定位对实时性要求较高、AUV 算力有限，所以没有把 GNN 堆得太深，而是采用三层图卷积结构。

---

# 2. 前向传播之前：输入图是什么？

在这个论文里，每个时刻的数据是：

[
[x_t,y_t,v_t,a_t,\theta_t,delay_t]
]

它们分别表示：

```text
x_t：前一时刻东向坐标
y_t：前一时刻北向坐标
v_t：速度
a_t：加速度
θ_t：航向角
delay_t：与主 AUV 之间的水声传播时延
```

论文把这些变量构造成一张图：

[
G_t = {X_t, A_t}
]

其中：

```text
X_t：节点特征矩阵
A_t：邻接矩阵
```

可以把 6 个变量看成 6 个节点：

```text
节点 1：x
节点 2：y
节点 3：v
节点 4：a
节点 5：θ
节点 6：delay
```

邻接矩阵 (A_t) 表示这些变量之间是否存在关系。例如：

```text
x 和 v 有关系
y 和 v 有关系
v 和 a 有关系
a 和 θ 有关系
delay 和 x、y 有关系
```

所以前向传播不是简单把 6 个数丢进 MLP，而是先告诉网络：

> 这些变量不是孤立的，它们之间存在物理关系和统计关系。

---

# 3. 第一步：GCN 层在做什么？

## 3.1 GCN 的核心公式

GCN 的基本传播公式可以写成：

[
H^{(l+1)}
=========

\sigma
\left(
\tilde{A}H^{(l)}W^{(l)}
\right)
]

其中：

```text
H^(l)：第 l 层的节点表示
H^(0) = X，即输入节点特征
A~：归一化后的邻接矩阵
W^(l)：第 l 层可训练权重
σ：激活函数，例如 ReLU
```

论文中也说明，GNN 层相比普通神经网络多了邻接矩阵 (A)，可写成：

[
H=\sigma(AXW)
]

也就是说，GNN 的计算不只依赖输入特征 (X)，还依赖节点之间的连接关系 (A)。

---

## 3.2 直观理解：GCN 做的是“邻居信息融合”

以 (x) 节点为例。

如果邻接矩阵中规定：

```text
x 与 y 相连
x 与 v 相连
x 与 delay 相连
```

那么经过 GCN 后，(x) 节点的新表示不再只包含自己的信息，而是融合了：

```text
x 自身的信息
y 的信息
v 的信息
delay 的信息
```

可以理解为：

[
h_x^{new}
=========

f(h_x,h_y,h_v,h_{delay})
]

这就是 GCN 的核心作用：

> 根据邻接矩阵规定的图结构，把相关节点的信息聚合起来，形成新的节点表示。

在这篇论文中，GCN 层的意义就是：

```text
让位置、速度、加速度、航向角、delay 之间互相传递信息；
让模型学习这些变量之间的非线性关系；
实现多源异构传感器信息融合。
```

---

## 3.3 第一层 GCN 做什么？

第一层输入是原始图特征：

[
H^{(0)}=X
]

第一层 GCN：

[
Z^{(1)}
=======

\tilde{A}XW^{(1)}
]

这里发生了两件事。

第一，乘以 (W^{(1)})：

[
XW^{(1)}
]

表示把每个节点的原始特征映射到隐藏维度。例如从原始一维或低维特征映射到 128 维隐藏表示。

第二，乘以 (\tilde{A})：

[
\tilde{A}XW^{(1)}
]

表示按照邻接矩阵进行邻居信息聚合。

所以第一层 GCN 的作用是：

> 把原始变量变成初级图特征，让每个变量先融合一跳邻居的信息。

例如：

```text
x 节点开始吸收 v 和 delay 的信息；
v 节点开始吸收 x、y、a 的信息；
a 节点开始吸收 v、θ 的信息。
```

---

## 3.4 第二层 GCN 做什么？

第二层输入是第一层输出：

[
H^{(1)}
]

第二层 GCN：

[
Z^{(2)}
=======

\tilde{A}H^{(1)}W^{(2)}
]

第二层的意义是继续做图上的信息传播。

如果第一层聚合的是“一跳邻居”，第二层就可以间接融合“二跳邻居”的信息。

例如：

```text
x 第一层融合了 v；
v 第一层融合了 a；
第二层后，x 可以间接获得 a 的信息。
```

所以第二层 GCN 的作用是：

> 在第一层局部关系的基础上，进一步融合更高阶的变量关系。

---

## 3.5 第三层 GCN 做什么？

第三层：

[
Z^{(3)}
=======

\tilde{A}H^{(2)}W^{(3)}
]

第三层进一步扩大信息融合范围，使节点表示包含更丰富的关系信息。

但 GNN 不能无限加深。论文也说明，图神经网络层数太多会聚合过远邻居信息，使局部结构被稀释，还可能引发过拟合；考虑 AUV 定位实时性和算力限制，本文采用三层图卷积层。

所以三层 GCN 的整体含义是：

```text
第 1 层：学习直接相关变量之间的关系
第 2 层：学习间接相关变量之间的关系
第 3 层：形成更完整的多源融合表示
```

---

# 4. BN 层在前向传播中做什么？

GCN 输出后，会进入 BN 层。

BN 全称：

```text
Batch Normalization
批量归一化
```

论文中说，BN 层的核心原理是对每一层输入进行归一化处理，解决内部协变量偏移问题，并缓解梯度消失或爆炸、加快收敛、提升泛化能力。

BN 的计算可以写成：

[
\hat{z}
=======

\frac{z-\mu_B}{\sqrt{\sigma_B^2+\epsilon}}
]

然后：

[
y
=

\gamma \hat{z}+\beta
]

其中：

```text
z：GCN 输出的隐藏特征
μ_B：当前 batch 中该隐藏特征的均值
σ_B²：当前 batch 中该隐藏特征的方差
γ：可学习缩放参数
β：可学习平移参数
```

BN 在这里的作用不是直接处理原始速度、delay 的量纲差异；那个问题主要由输入标准化解决。

BN 处理的是：

> GCN 层输出后的隐藏特征分布。

例如第一层 GCN 后，某些隐藏通道可能数值很大，某些隐藏通道可能数值很小。如果直接送入下一层，会导致训练不稳定。

BN 会把这些隐藏特征调整到比较稳定的分布：

```text
均值接近 0
方差接近 1
```

然后再通过 (\gamma,\beta) 让网络自己学习是否需要放大或平移。

所以 BN 的作用可以总结为：

```text
稳定隐藏层特征分布；
加快训练收敛；
缓解梯度消失或爆炸；
让后续 GCN 层更容易学习；
带来一定正则化效果。
```

---

# 5. ReLU 在前向传播中做什么？

BN 后通常会接 ReLU 激活函数。

[
ReLU(x)=\max(0,x)
]

它的作用是引入非线性。

如果没有 ReLU，多个 GCN + FC 层叠加起来，本质上仍然接近一个线性变换，表达能力有限。

ReLU 做的是：

```text
正值保留
负值置 0
```

例如：

[
[-2, 0.5, 3, -1]
\rightarrow
[0,0.5,3,0]
]

在这个定位任务里，ReLU 的意义是：

> 让网络能够拟合传感器变量和位置坐标之间复杂的非线性关系。

因为 AUV 位置和速度、加速度、航向角、delay 之间不是简单线性关系，所以需要非线性激活。

---

# 6. Dropout 层在前向传播中做什么？

Dropout 是一种防止过拟合的方法。

论文中说明，Dropout 在训练时会随机选择一部分神经元置为 0，被屏蔽的神经元不参与本轮权重更新；下一轮再恢复并重新随机丢弃，这相当于每次训练不同的子网络，从而减少模型对特定神经元的依赖。

训练时：

[
h_{drop}
========

m \odot h
]

其中：

```text
m：随机 mask，元素为 0 或 1
⊙：逐元素乘法
```

如果某个位置被 dropout 掉：

```text
该神经元输出变成 0
```

直观理解：

> Dropout 会强迫网络不能只依赖某几个隐藏特征，而要让多个特征共同承担预测任务。

在这篇论文中，水下数据量相对较小，模型容易过拟合。因此 Dropout 很重要。

它的作用是：

```text
防止网络死记训练轨迹；
降低对某几个传感器特征或隐藏通道的过度依赖；
提高对测试轨迹的泛化能力；
降低过拟合风险。
```

---

# 7. FC 层在前向传播中做什么？

经过三层 GCN 后，网络已经得到融合后的图特征。

但 GCN 输出仍然是隐藏特征，不是最终坐标。

因此需要 FC 层：

[
\hat{Y}
=======

H^{(3)}W_{FC}+b_{FC}
]

输出：

[
\hat{Y}=[\hat{x}*{t+1},\hat{y}*{t+1}]
]

FC 层的作用是：

```text
把 GCN 学到的多源融合特征整合起来；
映射到二维坐标空间；
完成回归预测。
```

论文也说明，全连接层能够将卷积层输出加权组合生成新的特征，将局部特征整合成全局特征，最终输出位置坐标预测结果。

---

# 8. 损失函数：如何判断预测对不对？

网络输出预测坐标：

[
\hat{Y}=[\hat{x}*{t+1},\hat{y}*{t+1}]
]

真实标签来自 GNSS：

[
Y=[x_{t+1},y_{t+1}]
]

论文使用 MSE 损失：

[
L
=

\frac{1}{n}
\sum_{i=1}^{n}
(\hat{y}_i-y_i)^2
]

对于二维坐标，可以理解为：

[
L
=

(\hat{x}-x)^2+(\hat{y}-y)^2
]

MSE 的意义是：

> 预测位置和真实位置差得越远，损失越大；训练目标就是让这个损失不断变小。

论文选择 MSE，是因为 AUV 定位是一个回归任务。

---

# 9. 前向传播完整总结

把上面串起来，前向传播就是：

[
X,A
\rightarrow
GCN_1
\rightarrow
BN_1
\rightarrow
ReLU
\rightarrow
Dropout
\rightarrow
GCN_2
\rightarrow
BN_2
\rightarrow
ReLU
\rightarrow
Dropout
\rightarrow
GCN_3
\rightarrow
BN_3
\rightarrow
Dropout
\rightarrow
FC
\rightarrow
\hat{Y}
\rightarrow
Loss
]

其中每一层的功能是：

```text
GCN：
按照邻接矩阵聚合相关变量信息，学习传感器变量之间的关系。

BN：
稳定隐藏特征分布，让训练更快、更稳定。

ReLU：
引入非线性，让模型能拟合复杂定位关系。

Dropout：
训练时随机屏蔽部分神经元，防止过拟合。

FC：
把融合后的图特征映射成二维坐标。

MSE：
衡量预测坐标和真实坐标之间的差距。
```

---

# 10. 反向传播总体在做什么？

前向传播得到预测结果后，反向传播要回答一个问题：

> 哪些参数导致预测错了？应该怎么调整它们？

也就是根据损失函数 (L)，计算每个参数的梯度：

[
\frac{\partial L}{\partial W}
]

然后用优化器更新参数。

论文使用 Adam 优化器。Adam 的作用是结合动量和自适应学习率，让参数更新更平稳、更高效。

---

# 11. 反向传播第一步：从 MSE 开始

假设预测值是：

[
\hat{Y}
]

真实值是：

[
Y
]

MSE：

[
L=(\hat{Y}-Y)^2
]

那么对预测值的梯度是：

[
\frac{\partial L}{\partial \hat{Y}}
===================================

2(\hat{Y}-Y)
]

含义很直观：

```text
如果预测值比真实值大，就往小调；
如果预测值比真实值小，就往大调；
误差越大，调整力度越大。
```

这个误差信号会从输出层一路往前传。

---

# 12. 反向传播经过 FC 层

FC 层：

[
\hat{Y}=HW_{FC}+b_{FC}
]

反向传播会计算：

[
\frac{\partial L}{\partial W_{FC}}
]

[
\frac{\partial L}{\partial b_{FC}}
]

[
\frac{\partial L}{\partial H}
]

含义是：

```text
更新 FC 层的权重和偏置；
同时把误差信号传回前面的 GCN 特征。
```

FC 层学到的是：

> 哪些图特征应该对最终 x、y 坐标预测贡献更大。

例如某些隐藏特征可能对应“位置-delay 关系”，某些隐藏特征可能对应“速度-航向角关系”。FC 会学习如何把它们组合成最终位置。

---

# 13. 反向传播经过 Dropout 层

Dropout 在前向传播时生成了一个 mask：

[
m
]

前向：

[
h_{drop}=m\odot h
]

反向传播时：

[
\frac{\partial L}{\partial h}
=============================

m\odot
\frac{\partial L}{\partial h_{drop}}
]

意思是：

```text
前向传播中被保留的神经元，继续传梯度；
前向传播中被置 0 的神经元，本轮不传梯度。
```

所以 Dropout 在反向传播中的作用是：

> 只有本轮参与训练的子网络会被更新，被丢弃的神经元本轮不更新。

这会让网络不要过度依赖某几个固定神经元。

---

# 14. 反向传播经过 ReLU

ReLU：

[
y=\max(0,x)
]

它的导数是：

[
\frac{\partial y}{\partial x}
=============================

\begin{cases}
1, & x>0\
0, & x\leq 0
\end{cases}
]

所以反向传播经过 ReLU 时：

```text
如果前向时该神经元输出为正，梯度正常传回；
如果前向时该神经元输出为 0，梯度被截断。
```

这就是 ReLU 的门控作用。

在这个网络里，ReLU 会让一些无效或负响应特征被抑制，只保留对预测有用的正激活特征。

---

# 15. 反向传播经过 BN 层

BN 前向是：

[
\hat{z}
=======

\frac{z-\mu_B}{\sqrt{\sigma_B^2+\epsilon}}
]

[
y=\gamma\hat{z}+\beta
]

反向传播时，需要更新：

```text
γ：缩放参数
β：平移参数
```

同时还要把梯度传回 BN 之前的 GCN 输出 (z)。

BN 的反向传播比 FC 和 ReLU 复杂一些，因为每个样本的归一化结果依赖整个 batch 的均值和方差。

但是从理解角度，你可以把它看成：

```text
BN 会学习每个隐藏通道应该放大多少、平移多少；
同时把稳定后的误差信号传回 GCN 层。
```

BN 在反向传播中的重要作用是：

> 避免梯度尺度过大或过小，让前面 GCN 层接收到更稳定的梯度。

这就是为什么 BN 能缓解梯度消失或梯度爆炸。

---

# 16. 反向传播经过 GCN 层

这是最关键的部分。

GCN 前向：

[
Z
=

\tilde{A}HW
]

其中：

```text
H：上一层输入特征
W：当前 GCN 层权重
A~：归一化邻接矩阵
Z：GCN 输出
```

反向传播要计算三个方向的梯度。

---

## 16.1 对权重 W 的梯度

[
\frac{\partial L}{\partial W}
]

这个梯度告诉模型：

> 当前这层 GCN 的特征变换方式应该怎么改。

例如，如果模型发现 delay 对位置预测很重要，那么相关通道的权重会被调大。

---

## 16.2 对输入 H 的梯度

[
\frac{\partial L}{\partial H}
]

这个梯度会继续往前传给上一层。

如果当前是第 3 层 GCN，梯度会传给第 2 层。

如果当前是第 2 层 GCN，梯度会传给第 1 层。

如果当前是第 1 层 GCN，梯度会传到输入特征，但输入本身通常不更新。

---

## 16.3 对邻接矩阵 A 的梯度

这个要分情况。

如果邻接矩阵 (A) 是固定的 0/1 图结构，那么它不参与训练，反向传播不会更新 (A)。

如果论文实现中把 (A_{phy}) 的具体边权设置为可训练参数，那么也可以计算：

[
\frac{\partial L}{\partial A}
]

然后更新边权。

这时模型不仅能学习：

```text
节点特征怎么变换
```

还可以学习：

```text
变量之间的关系强弱应该是多少
```

例如模型可能学到：

```text
delay 和 x 的关系很重要；
a 和 θ 的关系也重要；
某些人工设置的边权需要降低。
```

但注意：论文中的连接模式本身主要还是由人工物理先验给定，模型最多是在此基础上调整边权强度。

---

# 17. 反向传播完整路径

完整反向传播可以写成：

```text
Loss
  ↓
FC 层：
更新 FC 权重和偏置
  ↓
Dropout：
只让前向保留的神经元传梯度
  ↓
BN：
更新 γ、β，并稳定地传回梯度
  ↓
GCN 第 3 层：
更新 W3，必要时更新边权
  ↓
Dropout / ReLU / BN
  ↓
GCN 第 2 层：
更新 W2
  ↓
Dropout / ReLU / BN
  ↓
GCN 第 1 层：
更新 W1
  ↓
输入 X / 邻接矩阵 A
```

然后 Adam 根据梯度更新参数：

[
\theta
\leftarrow
\theta
------

\eta
\cdot
\text{AdamGradient}
]

其中 (\theta) 包括：

```text
GCN 层权重 W1, W2, W3
BN 参数 γ, β
FC 层权重和偏置
可能还有可训练边权
```

---

# 18. 用一个具体例子串起来

假设某一时刻输入：

```text
x = 1000
y = 1500
v = 2
a = 0.1
θ = 30°
delay = 300
```

目标是预测下一时刻：

```text
x_{t+1}, y_{t+1}
```

## 前向传播

第一步，输入标准化：

```text
把 x、y、v、a、θ、delay 都变成尺度接近的数据
```

第二步，构建图：

```text
x 与 y、v、delay 相连
v 与 a 相连
a 与 θ 相连
delay 与 x、y 相连
```

第三步，第 1 层 GCN：

```text
x 节点融合 y、v、delay 信息
v 节点融合 x、y、a 信息
delay 节点融合 x、y 信息
```

第四步，BN：

```text
让这些隐藏特征分布稳定
```

第五步，ReLU：

```text
保留有效正响应，抑制负响应
```

第六步，Dropout：

```text
随机丢掉一部分隐藏特征，防止过拟合
```

第七步，第 2、3 层 GCN 继续融合更深层关系。

第八步，FC 输出：

```text
预测坐标：
x_hat, y_hat
```

第九步，计算 MSE：

```text
预测坐标和 GNSS 真值之间的误差
```

---

## 反向传播

如果预测结果偏离真实值：

```text
x_hat 比真实 x 大了
y_hat 比真实 y 小了
```

MSE 会产生误差信号：

```text
x 方向应该往小调
y 方向应该往大调
```

这个信号传回 FC，FC 调整如何组合图特征。

再传回 GCN，GCN 调整：

```text
速度特征应该怎样影响位置预测；
delay 特征应该怎样影响位置预测；
航向角和加速度之间的关系如何表达；
各个隐藏通道权重如何变化。
```

BN 调整隐藏通道的缩放和平移。

Dropout 保证不是每次都依赖同一组隐藏神经元。

最终经过多轮训练，模型学会：

```text
给定当前 x, y, v, a, θ, delay，
如何预测下一时刻 x, y。
```

---

# 19. 你可以这样理解三类层的分工

## GCN / GNN 层：负责“学关系”

它回答的是：

```text
哪些变量相关？
相关变量之间如何互相影响？
怎样把这些变量融合成定位特征？
```

在论文中，GCN 是核心层。

---

## BN 层：负责“稳训练”

它回答的是：

```text
当前层输出是不是数值分布太乱？
是不是某些隐藏通道太大或太小？
梯度传播是否稳定？
```

BN 不直接做定位，但让 GCN 更容易训练好。

---

## Dropout 层：负责“防过拟合”

它回答的是：

```text
模型是不是太依赖某几个神经元？
是不是把训练轨迹记住了？
测试时会不会泛化差？
```

Dropout 通过随机丢弃部分神经元，迫使网络学习更稳健的表示。

---

# 20. 最后总结

这篇论文中的 GNN 前向传播可以概括为：

> 将某一时刻的 (x,y,v,a,\theta,delay) 构成图数据，通过多层 GCN 按邻接矩阵聚合变量关系，经过 BN 稳定特征分布、ReLU 引入非线性、Dropout 防止过拟合，最后用 FC 层输出下一时刻二维位置。

反向传播可以概括为：

> 根据预测位置和 GNSS 真值之间的 MSE 误差，从 FC 层开始反向传递梯度，依次经过 Dropout、ReLU、BN 和 GCN，更新 GCN 权重、BN 参数、FC 参数以及可能的边权，使下一次预测误差更小。

一句话理解：

```text
GCN 负责学“变量之间怎么互相影响”；
BN 负责让“每层特征分布更稳定”；
Dropout 负责让“模型不要死记训练数据”；
FC 负责把“图特征变成坐标预测”；
MSE + 反向传播负责告诉模型“哪里错了、该怎么改”。
```

